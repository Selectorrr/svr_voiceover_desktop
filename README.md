# SVR Voiceover Desktop

Лёгкое кроссплатформенное GUI-приложение для запуска Docker-контейнера `selector/voiceover`. Помогает быстро сгенерировать озвучку по CSV без командной строки.

---

## 📦 Установка

### Windows
1. Скачайте инсталлятор `svr_voiceover_desktop Setup 1.0.0.exe` из релизов на GitHub
2. Запустите его — приложение установится в `%LOCALAPPDATA%\Programs\svr_voiceover_desktop`
3. Ярлык появится в меню «Пуск»

По умолчанию приложение запускается сразу после установки.

### Linux
- .deb-пакет  
  `sudo dpkg -i svr_voiceover_desktop_1.0.0_amd64.deb`  
  `sudo apt-get install -f`  # при необходимости установить зависимости
- AppImage  
  `chmod +x svr_voiceover_desktop-1.0.0.AppImage`  
  `./svr_voiceover_desktop-1.0.0.AppImage`

---

## 🚀 Быстрый старт

1. Запустите приложение
2. Введите **API-ключ** (можно получить у Telegram-бота `@SynthVoiceRuBot`)
3. Выберите **формат** (ogg, wav, flac)
4. (Опции) Нажмите «Доп. настройки» и укажите:
    - **CSV-разделитель** (по умолчанию `,`)
    - **Устройство**: GPU (CUDA) или CPU
    - **Размер партии**: число строк из CSV за один запрос (чтобы не превысить лимит 30 запросов/мин)
    - **Потоки**: число параллельных задач (каждый поток ≈12 ГБ видеопамяти)
    - **Рабочая папка**: примонтируется в контейнере в `/workspace/SynthVoiceRu/workspace`
5. Нажмите ▶️ **Запустить**
6. Внизу отобразятся логи, прогресс-бар покажет процент выполнения

---

## 🛑 Остановка

Во время работы доступна кнопка 🛑 **Стоп** — она корректно остановит и удалит контейнер.

---

## 📖 Лицензия

[MIT](LICENSE) — свободно используйте и дорабатывайте под себя.  
Спасибо, что выбрали SVR Voiceover!

**Сборка AppImage**

из под wsl:

очищаем кеши
```
rm -rf dist node_modules package-lock.json
```
собираем
```
docker run --rm -it   -v "$(pwd)":/project   -v ~/.cache/electron-builder:/home/builder/.cache/electron-builder   -w /project   node:18-bullseye   bash -lc "\
apt update && \
apt install -y git python3 make g++ squashfs-tools && \
npm install && \
npx electron-builder --linux --x64 \
"
```