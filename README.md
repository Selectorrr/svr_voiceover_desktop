# SVR Voiceover Desktop


<img width="573" height="731" alt="Снимок экрана 2026-01-10 в 10 59 01" src="https://github.com/user-attachments/assets/ac1ae386-64cc-4121-a194-247a084e561d" />

Лёгкое кроссплатформенное GUI-приложение для запуска Docker-контейнера `selector/voiceover`. Помогает быстро сгенерировать озвучку по CSV без командной строки.

---

## 📋 Минимальные требования

- **Операционная система**
    - Windows 10 или новее
    - macOS 10.15+
    - Любой современный Linux с ядром 4.15+

- **Docker Engine**
    - Версия 20.10+ (Docker Desktop на Windows/macOS или пакет `docker.io` в Linux)

- **Железо**
    - CPU с 4+ ядрами
    - ОЗУ: минимум 8 ГБ, рекомендовано 16 ГБ+
    - **GPU-режим**: нужна CUDA
        - NVIDIA GPU (желательно 12 ГБ+ VRAM)
        - Установленные драйвера и совместимая CUDA

- **Диск**
    - ~2 ГБ под Docker-образ и временные файлы

- **Сеть и ключи**
    - Интернет для первого `docker pull`
    - API-ключ от Telegram-бота `@SynthVoiceRuBot`

---

## 📦 Установка

### Windows
1. Скачайте инсталлятор `svr_voiceover_desktop Setup 1.0.0.exe` из релизов на GitHub

2. Запустите его — приложение установится в `%LOCALAPPDATA%\Programs\svr_voiceover_desktop`
3. Ярлык появится в меню «Пуск»

По умолчанию приложение запускается сразу после установки.

### Linux
- .deb-пакет  
  `sudo dpkg -i svr_voiceover_desktop_1.0.0_amd64.deb`  
  `sudo apt-get install -f`  # при необходимости установить зависимости
- AppImage  
  `chmod +x svr_voiceover_desktop-1.0.0.AppImage`  
  `./svr_voiceover_desktop-1.0.0.AppImage`

---

## 🚀 Быстрый старт

1. Запустите приложение
2. Введите **API-ключ** (получить у `@SynthVoiceRuBot`)
3. Выберите **формат** результата (ogg / wav / flac)
4. Укажите **CSV-разделитель** (по умолчанию `,`) — поле на основной форме
5. Выберите **устройство**:
    - **GPU (CUDA)** — быстрее, требует VRAM
    - **CPU** — медленнее, но работает почти везде
6. (Опционально) Настройте параметры в аккордеоне:
    - **VC и просодия** (этот раздел открыт по умолчанию)
        - **Тип конверсии голоса (VC тип)**:
            - `default`
            - `native_bigvgan`
            - `native_vocos`
    - **Техническое**
        - **Размер партии** — сколько строк CSV обрабатывать за один проход (полезно для контроля нагрузки/лимитов)
        - **Потоки** — параллельность обработки (есть режим **Авто**)
        - **Reinit every** — как часто перезагружать ONNX-сессию. Нужно для очистки VRAM, если видеопамяти не хватает. `0` — выключено.
7. Нажмите ▶️ **Запустить**
8. Внизу появятся логи, прогресс-бар покажет процент выполнения

💡 Настройки сохраняются локально — после перезапуска приложения ничего заново набивать не нужно.

---

## 🛑 Остановка

Во время работы доступна кнопка 🛑 **Стоп** — она корректно остановит и удалит контейнер.

---

## 📖 Лицензия

[Apache 2.0](https://www.apache.org/licenses/LICENSE-2.0.txt) — свободно используйте и дорабатывайте под себя. Спасибо, что выбрали SVR Voiceover!

---

## 🧱 Сборка

### Сборка Exe (Windows)
Запускать из `cmd` с правами администратора:
```bash
npm install
npx electron-builder --win --x64
```

### Сборка AppImage (Linux)
Запускать из PowerShell:
```powershell
rm -rf node_modules package-lock.json
docker run --rm -it -v "${PWD}:/project" -v "$env:USERPROFILE\.cache\electron-builder:/home/builder/.cache/electron-builder" -w /project node:18-bullseye bash -lc "apt update && apt install -y git python3 make g++ squashfs-tools && npm install && npx electron-builder --linux --x64"
```
